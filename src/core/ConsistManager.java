package core;
import java.util.ArrayList;
import java.util.HashSet;
import java.util.LinkedList;
public class ConsistManager {
    public ArrayList<String> createArrayListConsist() { return new ArrayList<>(); }
    public HashSet<String> createHashSetConsist() { return new HashSet<>(); }
    public LinkedList<String> createLinkedListConsist() { return new LinkedList<>(); }
}